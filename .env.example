# Optional — most settings are configured through the dashboard setup wizard
# These env vars are only needed if you want to override defaults

# Port for the dashboard (default: 9222)
# PORT=9222

# Database path inside container (default: ./data/agents.db)
# DB_PATH=/app/data/agents.db
